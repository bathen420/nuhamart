# Changelog — v0.9.1 Stability Fix

- Changed anonymous migration `$withinTransaction` from typed `public bool` to framework-compatible untyped `public` property.
- Added explicit `Throwable` import.
- Added Windows PowerShell recovery automation for locked esbuild/Vite dependencies.
- Added build and test verification instructions.
