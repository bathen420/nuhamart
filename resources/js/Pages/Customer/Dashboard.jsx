import { Head, Link } from "@inertiajs/react";
import { CircleCheck, Clock3, Package, Wallet } from "lucide-react";
import CustomerAccountLayout from "@/Layouts/CustomerAccountLayout";
const money=v=>`৳${Number(v||0).toLocaleString('en-BD')}`;
export default function Dashboard({summary,recentOrders}){const cards=[["Orders",summary.orders,Package],["In progress",summary.pending,Clock3],["Delivered",summary.delivered,CircleCheck],["Total spent",money(summary.spent),Wallet]];return <CustomerAccountLayout title="My Dashboard"><Head title="My Account"/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value,Icon])=><div key={label} className="rounded-2xl bg-white p-5 shadow-sm"><Icon className="text-teal-700"/><p className="mt-4 text-sm font-bold text-slate-500">{label}</p><p className="mt-1 text-2xl font-black">{value}</p></div>)}</div><div className="mt-6 rounded-2xl bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><h2 className="text-xl font-black">Recent orders</h2><Link href={route('customer.orders.index')} className="text-sm font-black text-teal-700">View all →</Link></div><div className="mt-4 divide-y">{recentOrders.length?recentOrders.map(o=><Link key={o.id} href={route('customer.orders.show',o.id)} className="flex items-center justify-between gap-4 py-4"><div><b>{o.order_no}</b><p className="text-xs text-slate-500">{new Date(o.ordered_at).toLocaleDateString()}</p></div><div className="text-right"><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold capitalize">{o.status}</span><p className="mt-1 font-black">{money(o.total)}</p></div></Link>):<p className="py-10 text-center text-slate-500">No orders yet.</p>}</div></div></CustomerAccountLayout>}
