import { Head, Link } from "@inertiajs/react";
import CustomerAccountLayout from "@/Layouts/CustomerAccountLayout";
const money=v=>`৳${Number(v||0).toLocaleString('en-BD')}`;
export default function Orders({orders}){return <CustomerAccountLayout title="My Orders"><Head title="My Orders"/><div className="overflow-hidden rounded-2xl bg-white shadow-sm"><div className="border-b p-6"><h2 className="text-xl font-black">Order history</h2></div><div className="divide-y">{orders.data.map(o=><Link key={o.id} href={route('customer.orders.show',o.id)} className="grid gap-3 p-5 hover:bg-slate-50 sm:grid-cols-4 sm:items-center"><div><b>{o.order_no}</b><p className="text-xs text-slate-500">{new Date(o.ordered_at).toLocaleString()}</p></div><span className="text-sm">{o.items_count} item(s)</span><span className="w-fit rounded-full bg-teal-50 px-3 py-1 text-xs font-black capitalize text-teal-700">{o.status}</span><b className="sm:text-right">{money(o.total)}</b></Link>)}</div>{!orders.data.length&&<p className="p-12 text-center text-slate-500">No orders found.</p>}</div></CustomerAccountLayout>}
