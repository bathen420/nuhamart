import { Head } from "@inertiajs/react";
import { CheckCircle2, Circle } from "lucide-react";
import CustomerAccountLayout from "@/Layouts/CustomerAccountLayout";
const money=v=>`৳${Number(v||0).toLocaleString('en-BD')}`;
const stages=['pending','confirmed','processing','shipped','delivered'];
export default function Show({order}){const current=Math.max(0,stages.indexOf(order.status));return <CustomerAccountLayout title={order.order_no}><Head title={order.order_no}/><div className="space-y-6"><div className="rounded-2xl bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Order progress</h2><div className="mt-6 grid gap-3 sm:grid-cols-5">{stages.map((s,i)=><div key={s} className="flex items-center gap-2 sm:block">{i<=current?<CheckCircle2 className="text-teal-600"/>:<Circle className="text-slate-300"/>}<p className={`mt-2 text-sm font-bold capitalize ${i<=current?'text-slate-900':'text-slate-400'}`}>{s}</p></div>)}</div></div><div className="grid gap-6 lg:grid-cols-[1fr_340px]"><div className="rounded-2xl bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Items</h2><div className="mt-4 divide-y">{order.items.map((i,n)=><div key={n} className="flex justify-between gap-4 py-4"><span>{i.name} × {i.quantity}</span><b>{money(i.subtotal)}</b></div>)}</div></div><aside className="rounded-2xl bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Summary</h2><div className="mt-4 space-y-3 text-sm"><div className="flex justify-between"><span>Subtotal</span><b>{money(order.subtotal)}</b></div><div className="flex justify-between"><span>Shipping</span><b>{money(order.shipping_charge)}</b></div><div className="flex justify-between border-t pt-3 text-lg"><b>Total</b><b className="text-teal-700">{money(order.total)}</b></div><p className="pt-3 text-slate-500">Deliver to: {order.address}</p><p className="capitalize">Payment: {order.payment_method} · {order.payment_status}</p></div></aside></div></div></CustomerAccountLayout>}
