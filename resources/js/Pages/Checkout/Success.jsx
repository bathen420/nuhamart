import { Head, Link } from "@inertiajs/react";
import { CheckCircle2 } from "lucide-react";
import StorefrontLayout from "@/Layouts/StorefrontLayout";
const money=(v)=>`৳${Number(v||0).toLocaleString("en-BD",{maximumFractionDigits:2})}`;
export default function Success({order}){return <StorefrontLayout><Head title={`Order ${order.order_no}`}/><main className="bg-slate-50 py-14"><div className="mx-auto max-w-3xl px-4"><div className="rounded-3xl bg-white p-7 shadow-sm sm:p-10"><CheckCircle2 className="h-16 w-16 text-green-500"/><p className="mt-5 text-sm font-bold uppercase tracking-[0.2em] text-green-600">Order confirmed</p><h1 className="mt-2 text-3xl font-black">Thank you, {order.customer_name}!</h1><p className="mt-3 text-slate-600">Your order <b>{order.order_no}</b> has been received. We may contact you at {order.phone} to confirm delivery.</p><div className="mt-7 space-y-3 rounded-2xl bg-slate-50 p-5">{order.items.map((item,i)=><div key={i} className="flex justify-between gap-4 text-sm"><span>{item.name} × {item.quantity}</span><b>{money(item.subtotal)}</b></div>)}<div className="border-t pt-3"><div className="flex justify-between text-sm"><span>Shipping</span><b>{money(order.shipping_charge)}</b></div><div className="mt-2 flex justify-between text-lg"><span className="font-black">Total</span><span className="font-black text-orange-600">{money(order.total)}</span></div></div></div><div className="mt-7 flex flex-wrap gap-3"><Link href={route("storefront.catalog")} className="rounded-xl bg-orange-600 px-6 py-3 font-bold text-white">Continue shopping</Link><Link href={route("home")} className="rounded-xl border px-6 py-3 font-bold">Back to homepage</Link></div></div></div></main></StorefrontLayout>}
