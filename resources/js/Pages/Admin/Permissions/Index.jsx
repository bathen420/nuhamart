import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';import {Head} from '@inertiajs/react';export default function Index({permissionGroups}){return <AuthenticatedLayout header={<div><h2 className="text-xl font-semibold">Permission Catalogue</h2><p className="text-sm text-gray-500">System permissions are managed through roles.</p></div>}><Head title="Permissions"/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{Object.entries(permissionGroups).map(([g,ps])=><div className="rounded-xl border bg-white p-5" key={g}><h3 className="mb-3 font-bold capitalize">{g.replaceAll('-',' ')}</h3><div className="flex flex-wrap gap-2">{ps.map(p=><span key={p.id} className="rounded-full bg-gray-100 px-3 py-1 text-xs">{p.name}</span>)}</div></div>)}</div></AuthenticatedLayout>}
