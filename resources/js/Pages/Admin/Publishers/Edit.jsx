import {Head,useForm} from "@inertiajs/react"; import AuthenticatedLayout from "@/Layouts/AuthenticatedLayout"; import Form from "./Form";
export default function Edit({auth,publisher}){const {data,setData,put,processing,errors}=useForm({name:publisher.name??"",name_bn:publisher.name_bn??"",description:publisher.description??"",description_bn:publisher.description_bn??"",status:publisher.status?1:0,sort_order:publisher.sort_order??0});const submit=e=>{e.preventDefault();put(route("admin.publishers.update",publisher.id));};return <AuthenticatedLayout user={auth?.user} header={<h2 className="text-xl font-semibold">Edit Publisher</h2>}><Head title="Edit Publisher"/><div className="mx-auto max-w-4xl py-8"><div className="rounded-lg bg-white p-6 shadow"><Form {...{data,setData,errors,processing,submit}} buttonText="Update Publisher"/></div></div></AuthenticatedLayout>}
