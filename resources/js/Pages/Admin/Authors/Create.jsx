import {Head,useForm} from "@inertiajs/react"; import AuthenticatedLayout from "@/Layouts/AuthenticatedLayout"; import Form from "./Form";
export default function Create({auth}){const {data,setData,post,processing,errors}=useForm({name:"",name_bn:"",biography:"",biography_bn:"",status:1,sort_order:0});const submit=e=>{e.preventDefault();post(route("admin.authors.store"));};return <AuthenticatedLayout user={auth?.user} header={<h2 className="text-xl font-semibold">Add Author</h2>}><Head title="Add Author"/><div className="mx-auto max-w-4xl py-8"><div className="rounded-lg bg-white p-6 shadow"><Form {...{data,setData,errors,processing,submit}} buttonText="Save Author"/></div></div></AuthenticatedLayout>}
