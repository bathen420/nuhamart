import { Head, Link, router, useForm } from "@inertiajs/react";
import AuthenticatedLayout from "@/Layouts/AuthenticatedLayout";

export default function Resume({auth,hold}){return <AuthenticatedLayout user={auth?.user} header={<h2 className="text-xl font-semibold">Resume {hold.reference}</h2>}><Head title="Resume Held Sale"/><div className="mx-auto max-w-4xl rounded-xl bg-white p-6 shadow"><p className="mb-4 text-gray-600">Customer: {hold.customer?.name??'Walk-in Customer'}</p><div className="space-y-2">{hold.items.map(i=><div key={i.id} className="flex justify-between border-b py-3"><span>{i.product?.name} × {i.quantity}</span><span>৳{(Number(i.price)*i.quantity).toFixed(2)}</span></div>)}</div><p className="mt-5 rounded bg-amber-50 p-3 text-sm">Use this item list to restore the cart in POS. Full browser-local automatic cart restore is intentionally avoided so stock is revalidated before checkout.</p><Link href={route('admin.pos.create')} className="mt-4 inline-block rounded bg-blue-600 px-4 py-2 text-white">Open POS</Link></div></AuthenticatedLayout>}
