import AuthenticatedLayout from "@/Layouts/AuthenticatedLayout";import {Head,router} from "@inertiajs/react";
export default function Show({transfer}){return <AuthenticatedLayout header={<h2 className="text-xl font-bold">{transfer.reference}</h2>}><Head title={transfer.reference}/><div className="space-y-5"><div className="rounded-xl bg-white p-5 shadow"><div className="grid gap-3 md:grid-cols-5"><div><b>From:</b> {transfer.from_warehouse?.name}</div><div><b>To:</b> {transfer.to_warehouse?.name}</div><div><b>Date:</b> {transfer.transfer_date}</div><div><b>Status:</b> <span className="capitalize">{transfer.status}</span></div><div>{transfer.status==="draft"&&<button onClick={()=>router.patch(route("admin.stock-transfers.dispatch",transfer.id))} className="rounded bg-amber-600 px-4 py-2 text-white">Dispatch</button>}{transfer.status==="dispatched"&&<button onClick={()=>router.patch(route("admin.stock-transfers.receive",transfer.id))} className="rounded bg-green-600 px-4 py-2 text-white">Receive</button>}</div></div></div><div className="rounded-xl bg-white shadow"><table className="w-full"><thead><tr><th className="p-3 text-left">Product</th><th>Quantity</th><th>Unit Cost</th></tr></thead><tbody>{transfer.items.map(i=><tr className="border-t" key={i.id}><td className="p-3">{i.product?.name}</td><td className="text-center">{i.quantity}</td><td className="text-center">{i.unit_cost}</td></tr>)}</tbody></table></div></div></AuthenticatedLayout>}