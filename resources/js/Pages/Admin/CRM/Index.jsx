import { Head, Link, router } from '@inertiajs/react';
import { useState } from 'react';
import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
const money=(v)=>Number(v??0).toLocaleString('en-BD',{minimumFractionDigits:2,maximumFractionDigits:2});
export default function Index({customers,filters={},summary={}}){const[search,setSearch]=useState(filters.search??'');const submit=e=>{e.preventDefault();router.get(route('admin.crm.index'),{search},{preserveState:true,replace:true});};return <AuthenticatedLayout header={<h2 className="text-xl font-semibold">CRM & Loyalty</h2>}><Head title="CRM & Loyalty"/><div className="space-y-5"><div className="grid gap-4 md:grid-cols-4">{[['Customers',summary.customers],['Members',summary.members],['Points',summary.points],['Wallet',`৳${money(summary.wallet)}`]].map(([a,b])=><div key={a} className="rounded-xl bg-white p-5 shadow"><div className="text-sm text-gray-500">{a}</div><div className="mt-2 text-2xl font-bold">{b??0}</div></div>)}</div><form onSubmit={submit} className="flex gap-2 rounded-xl bg-white p-4 shadow"><input className="w-full rounded-lg border-gray-300" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search name, phone or customer code"/><button className="rounded-lg bg-blue-600 px-5 py-2 text-white">Search</button></form><div className="overflow-hidden rounded-xl bg-white shadow"><div className="overflow-x-auto"><table className="min-w-full divide-y"><thead className="bg-gray-50"><tr>{['Customer','Phone','Tier','Points','Wallet','Orders','Lifetime Value','Action'].map(x=><th key={x} className="px-4 py-3 text-left text-xs uppercase text-gray-600">{x}</th>)}</tr></thead><tbody className="divide-y">{(customers?.data??[]).map(c=><tr key={c.id}><td className="px-4 py-4 font-medium">{c.name}<div className="text-xs text-gray-500">{c.customer_code}</div></td><td className="px-4 py-4">{c.phone}</td><td className="px-4 py-4">{c.crm_profile?.tier?.name??'Bronze'}</td><td className="px-4 py-4">{c.crm_profile?.points_balance??0}</td><td className="px-4 py-4">৳{money(c.crm_profile?.wallet_balance)}</td><td className="px-4 py-4">{c.sales_count??0}</td><td className="px-4 py-4">৳{money(c.lifetime_value)}</td><td className="px-4 py-4"><Link className="rounded bg-blue-600 px-3 py-2 text-sm text-white" href={route('admin.crm.show',c.id)}>View</Link></td></tr>)}</tbody></table></div></div></div></AuthenticatedLayout>}
