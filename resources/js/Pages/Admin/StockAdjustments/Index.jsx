import AuthenticatedLayout from "@/Layouts/AuthenticatedLayout";
import { Head, Link, router } from "@inertiajs/react";
export default function Index({adjustments,filters}){const update=(k,v)=>router.get(route("admin.stock-adjustments.index"),{...filters,[k]:v},{preserveState:true,replace:true});
return <AuthenticatedLayout header={<div className="flex items-center justify-between"><h2 className="text-xl font-bold">Stock Adjustments</h2><Link className="rounded bg-blue-600 px-4 py-2 text-white" href={route("admin.stock-adjustments.create")}>New Adjustment</Link></div>}><Head title="Stock Adjustments"/>
<div className="mb-4 flex gap-3"><input className="rounded border px-3 py-2" placeholder="Search reference" defaultValue={filters.search||""} onChange={e=>update("search",e.target.value)}/><select className="rounded border px-3 py-2" value={filters.status||""} onChange={e=>update("status",e.target.value)}><option value="">All status</option><option value="draft">Draft</option><option value="approved">Approved</option><option value="cancelled">Cancelled</option></select></div>
<div className="overflow-hidden rounded-xl bg-white shadow"><table className="w-full"><thead className="bg-gray-50"><tr><th className="p-3 text-left">Reference</th><th className="p-3 text-left">Warehouse</th><th className="p-3">Date</th><th className="p-3">Status</th><th className="p-3"></th></tr></thead><tbody>{adjustments.data.map(x=><tr key={x.id} className="border-t"><td className="p-3 font-semibold">{x.reference}</td><td className="p-3">{x.warehouse?.name}</td><td className="p-3 text-center">{x.adjustment_date}</td><td className="p-3 text-center capitalize">{x.status}</td><td className="p-3 text-right"><Link className="text-blue-600" href={route("admin.stock-adjustments.show",x.id)}>View</Link></td></tr>)}</tbody></table></div></AuthenticatedLayout>}