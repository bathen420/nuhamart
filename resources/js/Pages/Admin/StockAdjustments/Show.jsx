import AuthenticatedLayout from "@/Layouts/AuthenticatedLayout";import {Head,router} from "@inertiajs/react";
export default function Show({adjustment}){return <AuthenticatedLayout header={<h2 className="text-xl font-bold">{adjustment.reference}</h2>}><Head title={adjustment.reference}/><div className="space-y-5"><div className="rounded-xl bg-white p-5 shadow"><div className="grid gap-3 md:grid-cols-4"><div><b>Warehouse:</b> {adjustment.warehouse?.name}</div><div><b>Date:</b> {adjustment.adjustment_date}</div><div><b>Status:</b> <span className="capitalize">{adjustment.status}</span></div><div>{adjustment.status==="draft"&&<button onClick={()=>router.patch(route("admin.stock-adjustments.approve",adjustment.id))} className="rounded bg-green-600 px-4 py-2 text-white">Approve</button>}</div></div></div><div className="rounded-xl bg-white shadow"><table className="w-full"><thead><tr><th className="p-3 text-left">Product</th><th>Direction</th><th>Qty</th><th>Reason</th></tr></thead><tbody>{adjustment.items.map(i=><tr className="border-t" key={i.id}><td className="p-3">{i.product?.name}</td><td className="text-center capitalize">{i.direction}</td><td className="text-center">{i.quantity}</td><td className="text-center">{i.reason}</td></tr>)}</tbody></table></div></div></AuthenticatedLayout>}