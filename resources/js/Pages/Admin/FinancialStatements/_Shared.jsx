import { Link, router } from '@inertiajs/react';
export const money=(v)=>new Intl.NumberFormat('en-BD',{minimumFractionDigits:2,maximumFractionDigits:2}).format(Number(v||0));
export function DateFilter({filters,balance=false}){const submit=e=>{e.preventDefault();const f=new FormData(e.currentTarget);router.get(window.location.pathname,Object.fromEntries(f),{preserveState:true,replace:true});};return <form onSubmit={submit} className="print:hidden flex flex-wrap gap-3 rounded-xl border bg-white p-4 shadow-sm">{balance?<label className="text-sm">As of<input name="as_of" type="date" defaultValue={filters.as_of} className="ml-2 rounded-lg border-gray-300"/></label>:<><label className="text-sm">From<input name="date_from" type="date" defaultValue={filters.date_from} className="ml-2 rounded-lg border-gray-300"/></label><label className="text-sm">To<input name="date_to" type="date" defaultValue={filters.date_to} className="ml-2 rounded-lg border-gray-300"/></label></>}<button className="rounded-lg bg-blue-600 px-4 py-2 text-white">Apply</button><button type="button" onClick={()=>window.print()} className="rounded-lg bg-gray-700 px-4 py-2 text-white">Print</button></form>}
export function ExportButton({statement,filters}){return <Link href={route('admin.financial-statements.export',{statement,...filters})} className="print:hidden rounded-lg bg-green-600 px-4 py-2 text-sm font-semibold text-white">Export CSV</Link>}
const toneClasses={blue:'text-blue-700',green:'text-green-700',purple:'text-purple-700',amber:'text-amber-700',red:'text-red-700'};
export function Card({label,value,tone='blue'}){return <div className="rounded-xl border bg-white p-5 shadow-sm"><p className="text-sm text-gray-500">{label}</p><p className={`mt-2 text-2xl font-bold ${toneClasses[tone]||toneClasses.blue}`}>৳{money(value)}</p></div>}
export function ReportHeader({title,subtitle,children}){return <div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h1 className="text-2xl font-bold text-gray-900">{title}</h1><p className="text-sm text-gray-500">{subtitle}</p></div>{children}</div>}
