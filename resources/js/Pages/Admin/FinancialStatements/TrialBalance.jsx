import {Head} from '@inertiajs/react';import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';import {DateFilter,ExportButton,ReportHeader,money} from './_Shared';
export default function TrialBalance({filters,rows=[],totals}){return <AuthenticatedLayout header={<h2 className="text-xl font-semibold">Trial Balance</h2>}><Head title="Trial Balance"/><div className="space-y-5"><ReportHeader title="Trial Balance" subtitle={`${filters.date_from} to ${filters.date_to}`}><ExportButton statement="trial-balance" filters={filters}/></ReportHeader><DateFilter filters={filters}/><div className="overflow-x-auto rounded-xl border bg-white shadow-sm"><table className="min-w-full text-sm"><thead className="bg-gray-50"><tr>{['Code','Account','Opening Dr','Opening Cr','Period Dr','Period Cr','Closing Dr','Closing Cr'].map(h=><th key={h} className="p-3 text-right first:text-left">{h}</th>)}</tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-t"><td className="p-3">{r.code}</td><td className="p-3">{r.name}</td>{['opening_debit','opening_credit','period_debit','period_credit','closing_debit','closing_credit'].map(k=><td key={k} className="p-3 text-right">{money(r[k])}</td>)}</tr>)}</tbody><tfoot className="bg-gray-50 font-bold"><tr><td colSpan="2" className="p-3">TOTAL</td>{['opening_debit','opening_credit','period_debit','period_credit','closing_debit','closing_credit'].map(k=><td key={k} className="p-3 text-right">{money(totals[k])}</td>)}</tr></tfoot></table></div></div></AuthenticatedLayout>}
