import AuthenticatedLayout from "@/Layouts/AuthenticatedLayout"; import {Head,Link,router} from "@inertiajs/react"; import {useState} from "react";
export default function Index({auth,returns,filters}){const [search,setSearch]=useState(filters.search||'');const submit=e=>{e.preventDefault();router.get(route('admin.purchase-returns.index'),{search},{preserveState:true,replace:true})};return <AuthenticatedLayout user={auth.user}><Head title="Purchase Returns"/><div className="py-8"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><h1 className="mb-6 text-3xl font-bold">Purchase Returns</h1><form onSubmit={submit} className="mb-6 flex gap-2"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Return no, purchase or supplier..." className="w-full max-w-md rounded-lg border-gray-300"/><button className="rounded-lg bg-blue-600 px-5 py-2 font-semibold text-white">Search</button></form><div className="overflow-hidden rounded-xl bg-white shadow"><table className="w-full"><thead className="bg-gray-100"><tr><th className="px-4 py-3 text-left">Return No.</th><th className="text-left">Purchase</th><th className="text-left">Supplier</th><th className="text-left">Date</th><th className="text-right">Amount</th><th>Action</th></tr></thead><tbody>{returns.data.map(i=><tr key={i.id} className="border-t"><td className="px-4 py-4 font-semibold">{i.return_number}</td><td>{i.purchase?.purchase_number}</td><td>{i.supplier?.name||'—'}</td><td>{i.return_date}</td><td className="text-right font-bold">BDT {Number(i.subtotal).toLocaleString('en-GB',{minimumFractionDigits:2})}</td><td className="text-center"><Link href={route('admin.purchase-returns.show',i.id)} className="text-blue-600 hover:underline">View</Link></td></tr>)}{returns.data.length===0&&<tr><td colSpan="6" className="px-4 py-10 text-center text-gray-500">No purchase return found.</td></tr>}</tbody></table></div></div></div></AuthenticatedLayout>}
